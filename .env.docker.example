# Copy to .env.docker for docker compose (gitignored).
MYSQL_ROOT_PASSWORD=changeme_dev_only
JWT_SECRET=change-me-to-a-long-random-secret-for-docker

# Optional overrides (defaults shown in docker-compose.yml)
# ADMIN_USER_IDS=
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
