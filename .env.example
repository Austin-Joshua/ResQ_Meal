# Frontend API base path (Vite dev server proxies /api → Spring Boot on 8080).
# VITE_API_URL=/api
# Real-time Socket.IO (Spring netty-socketio; default port 9090).
# VITE_SOCKET_URL=http://localhost:9090
