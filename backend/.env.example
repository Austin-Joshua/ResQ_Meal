# ==================== SERVER ====================
NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:8080

# ==================== DATABASE (MySQL) ====================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=resqmeal_db
DB_USER=root
DB_PASSWORD=your_password
DB_POOL_LIMIT=10

# ==================== JWT ====================
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_make_it_long_and_random
JWT_EXPIRY=7d

# ==================== FILE UPLOAD ====================
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# ==================== FRESHNESS AI ====================
# Run: node scripts/setup-freshness-models.js (from repo root) to download trained models from GitHub.
# Then start the desired ml-services (see README "Freshness detector & ML models").
# Optional: Amazon Bedrock (Claude 3 vision) - tried first when set. Requires AWS credentials.
# AWS_REGION=us-east-1
# BEDROCK_FRESHNESS_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0
# Image-based (tried in order): TFLite, Roboflow, FreshVision, fruit-veg-freshness-ai.
# FRESHNESS_TFLITE_URL=http://localhost:8002
# FRESHNESS_ROBOFLOW_URL=http://localhost:8003
# FRESHNESS_FRESHVISION_URL=http://localhost:8004
# FRESHNESS_AI_URL=http://localhost:8000
# Environment-based: Food-Freshness-Analyzer (trains on startup; no model download).
# Run: cd ml-services/food-freshness-analyzer && pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8001
# FRESHNESS_ENV_AI_URL=http://localhost:8001
# Food classification + nutrition (Food-Image-Recognition).
# FOOD_IMAGE_RECOGNITION_URL=http://localhost:8005

# ==================== AWS (Optional - for production) ====================
AWS_ACCESS_KEY_ID=your_aws_key
AWS_SECRET_ACCESS_KEY=your_aws_secret
AWS_REGION=us-east-1
AWS_S3_BUCKET=resqmeal-uploads

# ==================== LOGGING ====================
LOG_LEVEL=info
